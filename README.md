# SchemaForge

Enterprise JSON Schema Designer & Document Validation Platform built with ASP.NET Core.

## Solution

- `SchemaForge.API`: ASP.NET Core backend service

## Getting Started

1. Install the .NET SDK required by the project.
2. From the repository root, run `dotnet run --project SchemaForge.API`.
